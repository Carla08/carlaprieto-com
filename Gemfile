source "https://rubygems.org"

# Usa el mismo set de gemas que GitHub Pages en producción,
# para que "bundle exec jekyll serve" se comporte igual que el sitio publicado.
gem "github-pages", group: :jekyll_plugins
